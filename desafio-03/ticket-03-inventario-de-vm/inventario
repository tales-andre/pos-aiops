#!/usr/bin/env python3
"""Ponto de entrada da ferramenta: `./inventario --host ... --usuario ... --chave ...`

E so um invólucro. Toda a logica esta em src/inventario/.
"""

import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), "src"))

from inventario.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
